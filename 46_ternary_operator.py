# in this code we will see the if-else condition using another method called ternary operator

is_male = True

# below is the ternary operator using if else in one line
drive = 'You can drive' if is_male else 'Woman hhhhhhhhhhhhh'

# now printing the message
print(drive)
